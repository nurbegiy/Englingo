# Supabase project settings
# Leave both empty to run the app in DEMO MODE with local mock data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: force demo mode even if Supabase keys are present
VITE_DEMO_MODE=false
